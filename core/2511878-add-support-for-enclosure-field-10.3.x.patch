diff --git a/core/modules/views/config/schema/views.row.schema.yml b/core/modules/views/config/schema/views.row.schema.yml
index 3eae0c9354..f892569c2a 100644
--- a/core/modules/views/config/schema/views.row.schema.yml
+++ b/core/modules/views/config/schema/views.row.schema.yml
@@ -46,6 +46,12 @@ views.row.rss_fields:
     date_field:
       type: string
       label: 'Publication date field'
+    enclosure_field:
+      type: string
+      label: 'Enclosure field'
+    enclosure_field_alt:
+      type: string
+      label: 'Enclosure field Fallback'
     guid_field_options:
       type: mapping
       label: 'Guid settings'
diff --git a/core/modules/views/src/Plugin/views/row/RssFields.php b/core/modules/views/src/Plugin/views/row/RssFields.php
index 64cb267b5c..7789977b15 100644
--- a/core/modules/views/src/Plugin/views/row/RssFields.php
+++ b/core/modules/views/src/Plugin/views/row/RssFields.php
@@ -2,10 +2,17 @@

 namespace Drupal\views\Plugin\views\row;

+use Drupal\Core\Entity\Display\EntityDisplayInterface;
+use Drupal\Core\Entity\EntityTypeManagerInterface;
+use Drupal\Core\Field\EntityReferenceFieldItemList;
+use Drupal\Core\File\FileUrlGeneratorInterface;
 use Drupal\Core\Form\FormStateInterface;
 use Drupal\Core\StringTranslation\TranslatableMarkup;
-use Drupal\Core\Url;
 use Drupal\views\Attribute\ViewsRow;
+use Drupal\Core\Image\ImageFactory;
+use Drupal\Core\Url;
+use Drupal\file\Plugin\Field\FieldType\FileFieldItemList;
+use Symfony\Component\DependencyInjection\ContainerInterface;

 /**
  * Renders an RSS item based on fields.
@@ -19,6 +26,13 @@
 )]
 class RssFields extends RowPluginBase {

+  /**
+   * The image style manager.
+   *
+   * @var \Drupal\Core\Image\ImageFactory
+   */
+  protected $imageFactory;
+
   /**
    * Does the row plugin support to add fields to its output.
    *
@@ -26,6 +40,65 @@ class RssFields extends RowPluginBase {
    */
   protected $usesFields = TRUE;

+  /**
+   * The entity type manager.
+   *
+   * @var \Drupal\Core\Entity\EntityTypeManagerInterface
+   */
+  protected $entityTypeManager;
+
+  /**
+   * The file url generator services.
+   *
+   * @var \Drupal\Core\File\FileUrlGeneratorInterface
+   */
+  protected $fileUrlGenerator;
+
+  /**
+   * Constructs a RssPluginBase  object.
+   *
+   * @param array $configuration
+   *   A configuration array containing information about the plugin instance.
+   * @param string $plugin_id
+   *   The plugin_id for the plugin instance.
+   * @param mixed $plugin_definition
+   *   The plugin implementation definition.
+   * @param \Drupal\Core\Entity\EntityTypeManagerInterface $entity_type_manager
+   *   The entity type manager.
+   * @param \Drupal\Core\Image\ImageFactory $image_factory
+   *   The image factory.
+   * @param \Drupal\Core\File\FileUrlGeneratorInterface $file_url_generator
+   *   The file url generator services.
+   */
+  public function __construct(
+    array $configuration,
+    $plugin_id,
+    $plugin_definition,
+    EntityTypeManagerInterface $entity_type_manager,
+    ImageFactory $image_factory,
+    FileUrlGeneratorInterface $file_url_generator
+  ) {
+    parent::__construct($configuration, $plugin_id, $plugin_definition);
+
+    $this->imageFactory = $image_factory;
+    $this->entityTypeManager = $entity_type_manager;
+    $this->fileUrlGenerator = $file_url_generator;
+  }
+
+  /**
+   * {@inheritdoc}
+   */
+  public static function create(ContainerInterface $container, array $configuration, $plugin_id, $plugin_definition) {
+    return new static(
+      $configuration,
+      $plugin_id,
+      $plugin_definition,
+      $container->get('entity_type.manager'),
+      $container->get('image.factory'),
+      $container->get('file_url_generator')
+    );
+  }
+
   protected function defineOptions() {
     $options = parent::defineOptions();
     $options['title_field'] = ['default' => ''];
@@ -33,6 +106,8 @@ protected function defineOptions() {
     $options['description_field'] = ['default' => ''];
     $options['creator_field'] = ['default' => ''];
     $options['date_field'] = ['default' => ''];
+    $options['enclosure_field'] = ['default' => ''];
+    $options['enclosure_field_alt'] = ['default' => ''];
     $options['guid_field_options']['contains']['guid_field'] = ['default' => ''];
     $options['guid_field_options']['contains']['guid_field_is_permalink'] = ['default' => TRUE];
     return $options;
@@ -85,6 +160,20 @@ public function buildOptionsForm(&$form, FormStateInterface $form_state) {
       '#default_value' => $this->options['date_field'],
       '#required' => TRUE,
     ];
+    $form['enclosure_field'] = [
+      '#type' => 'select',
+      '#title' => $this->t('Enclosure field'),
+      '#description' => $this->t('Describes a media object that is attached to the item. This must be a file or media field.'),
+      '#options' => $view_fields_labels,
+      '#default_value' => $this->options['enclosure_field'],
+    ];
+    $form['enclosure_field_alt'] = [
+      '#type' => 'select',
+      '#title' => $this->t('Enclosure field - fallback'),
+      '#description' => $this->t('Describes a media object that is attached to the item. This must be a file or media field. <em>Fallback if first enclosure field is empty</em>'),
+      '#options' => $view_fields_labels,
+      '#default_value' => $this->options['enclosure_field_alt'],
+    ];
     $form['guid_field_options'] = [
       '#type' => 'details',
       '#title' => $this->t('GUID settings'),
@@ -146,6 +235,107 @@ public function render($row) {
         'namespace' => ['xmlns:dc' => 'http://purl.org/dc/elements/1.1/'],
       ],
     ];
+
+    if ($this->options['enclosure_field']) {
+      $field_name = $this->options['enclosure_field'];
+      $field = $this->view->field[$field_name];
+      $field_options = $field->options;
+      $entity = $field->getEntity($this->view->result[$row_index]);
+      $enclosure = $entity->$field_name;
+      $enclosure_alt = FALSE;
+      if (!empty($this->options['enclosure_field_alt'])) {
+        $field_name_alt = $this->options['enclosure_field_alt'];
+        $field_alt = $this->view->field[$field_name_alt];
+        $field_options_alt = $field_alt->options;
+        $entity_alt = $field_alt->getEntity($this->view->result[$row_index]);
+        $enclosure_alt = $entity_alt->$field_name_alt;
+      }
+      $file = NULL;
+
+      if ($enclosure instanceof FileFieldItemList && $enclosure->count() > 0) {
+        $value = $enclosure->getValue();
+        $file = $this->entityTypeManager->getStorage('file')->load($value[0]['target_id']);
+      }
+      elseif ($enclosure instanceof EntityReferenceFieldItemList && count($enclosure->referencedEntities()) > 0) {
+        $field = $this->entityTypeManager->getStorage('field_config')->load($entity->getEntityTypeId() . '.' . $entity->bundle() . '.' . $field_name);
+        if (isset($field)) {
+          $field_storage = $this->entityTypeManager->getStorage('field_storage_config')->load($field->getTargetEntityTypeId() . '.' . $field->getName());
+          if ($field->getType() == 'entity_reference' && $field_storage->getSetting("target_type") === "media") {
+            $file = $enclosure->referencedEntities()[0]->get('thumbnail')->entity;
+          }
+        }
+      }
+
+      if (empty($file) && $enclosure_alt instanceof FileFieldItemList && $enclosure_alt->count() > 0) {
+        $value_alt = $enclosure_alt->getValue();
+        $file = $this->entityTypeManager->getStorage('file')->load($value_alt[0]['target_id']);
+        $field_options = $field_options_alt;
+      }
+      if (empty($file) && $enclosure_alt instanceof EntityReferenceFieldItemList && count($enclosure_alt->referencedEntities()) > 0) {
+        $field = $this->entityTypeManager->getStorage('field_config')->load($entity_alt->getEntityTypeId() . '.' . $entity_alt->bundle() . '.' . $field_name_alt);
+        if (isset($field)) {
+          $field_storage = $this->entityTypeManager->getStorage('field_storage_config')
+            ->load($field->getTargetEntityTypeId() . '.' . $field->getName());
+          if ($field->getType() == 'entity_reference' && $field_storage->getSetting("target_type") === "media") {
+            $file = $enclosure_alt->referencedEntities()[0]->get('thumbnail')->entity;
+            $field_options = $field_options_alt;
+          }
+        }
+      }
+
+      if (isset($file)) {
+        $file_url = '';
+        $file_size = '';
+        $file_mimetype = '';
+        $image_style_name = '';
+        $file_uri = $file->getFileUri();
+
+        if (!empty($field_options['settings']['image_style'])) {
+          $image_style_name = $field_options['settings']['image_style'];
+        }
+        elseif (!empty($field_options['settings']['view_mode'])) {
+          $media_view_mode = $this->entityTypeManager->getStorage('entity_view_display')->load('media.image.' . $field_options['settings']['view_mode']);
+          if ($media_view_mode instanceof EntityDisplayInterface) {
+            $image_style_name = $media_view_mode->getComponent('field_media_image')['settings']['image_style'] ?? '';
+          }
+        }
+
+        if (!empty($image_style_name)) {
+          $style = $this->entityTypeManager->getStorage('image_style')->load($image_style_name);
+          $derivative_uri = $style->buildUri($file_uri);
+          $derivative_exists = TRUE;
+          if (!file_exists($derivative_uri)) {
+            $derivative_exists = $style->createDerivative($file_uri,
+              $derivative_uri);
+          }
+          if ($derivative_exists) {
+            $image = $this->imageFactory->get($derivative_uri);
+            $file_url = $this->fileUrlGenerator->generateAbsoluteString($derivative_uri);
+            $file_size = $image->getFileSize();
+            $file_mimetype = $image->getMimeType();
+          }
+        }
+        else {
+          // In RSS feeds, it is necessary to use absolute URLs. The 'url.site'
+          // cache context is already associated with RSS feed responses, so it
+          // does not need to be specified here.
+          $file_url = $this->fileUrlGenerator->generateAbsoluteString($file_uri);
+          $file_size = $file->getSize();
+          $file_mimetype = $file->getMimeType();
+        }
+        if (!empty($file_url)) {
+          $item->elements[] = [
+            'key' => 'enclosure',
+            'attributes' => [
+              'url' => $file_url,
+              'length' => $file_size,
+              'type' => $file_mimetype,
+            ],
+          ];
+        }
+      }
+    }
+
     $guid_is_permalink_string = 'false';
     $item_guid = $this->getField($row_index, $this->options['guid_field_options']['guid_field']);
     if ($this->options['guid_field_options']['guid_field_is_permalink']) {
@@ -215,6 +405,11 @@ protected function getAbsoluteUrl($url_string) {
       // @see https://www.drupal.org/node/2423913
       return $host . $url_string;
     }
+    // If the given URL already starts with http:// or https://, it's been
+    // processed.
+    elseif (strpos($url_string, 'http://') === 0 || strpos($url_string, 'https://') === 0) {
+      return $url_string;
+    }
     // Otherwise, this is an unprocessed path (e.g. node/123) and we need to run
     // it through a Url object to allow outbound path processors to run (path
     // aliases, language prefixes, etc).
