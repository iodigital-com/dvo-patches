diff --git a/modules/openwoo_search/src/Controller/OpenWooSearchController.php b/modules/openwoo_search/src/Controller/OpenWooSearchController.php
index 55f0705bbd53ef864f807aad9cb31784ea05eb9d..b4ffd022584e01b517feb9d347db1324a3671a35 100644
--- a/modules/openwoo_search/src/Controller/OpenWooSearchController.php
+++ b/modules/openwoo_search/src/Controller/OpenWooSearchController.php
@@ -136,16 +136,7 @@ class OpenWooSearchController extends ControllerBase {
       if (!$document['status']) {
         continue;
       }
-      // All attachments should have the label 'woo_publicatie'.
-      if (!in_array('woo_publicatie', $document['labels'])) {
-        continue;
-      }
-      // If the label is null, only include documents that
-      // only have the 'woo_publicatie' label otherwise the label is used.
-      if (is_null($label) && $document['labels'] !== ['woo_publicatie']) {
-        continue;
-      }
-      elseif (!is_null($label) && !in_array($label, $document['labels'])) {
+      if (!is_null($label) && !in_array($label, $document['labels'])) {
         continue;
       }
 
